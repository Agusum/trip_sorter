<%= form_for(@boarding_card) do |f| %>
  <% if @boarding_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@boarding_card.errors.count, "error") %> prohibited this boarding_card from being saved:</h2>

      <ul>
      <% @boarding_card.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
